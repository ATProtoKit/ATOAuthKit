# Conversion for `oauth-types/src/oauth-response-mode.ts`

## `export const oauthResponseModeSchema`

This has been converted.

Swift equivalent:

```swift
public enum OAuthResponseMode: String, Codable
```
