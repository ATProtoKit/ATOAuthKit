# Conversion for `oauth-types/src/oauth-client-metadata.ts`

## `export const oauthClientMetadataSchema`

This has been converted.

Swift equivalent:

```swift
public struct ClientMetadata: Codable
```
