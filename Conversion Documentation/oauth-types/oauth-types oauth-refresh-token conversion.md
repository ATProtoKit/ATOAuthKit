# Conversion for `oauth-types/src/oauth-refresh-token.ts`

## `export const oauthRefreshTokenSchema`

This has been converted.

Swift equivalent:

```swift
public struct OAuthRefreshToken: RawRepresentable
```

This was converted into a `struct`.
