# Conversion for `oauth-types/src/oauth-issuer-identifier.ts`

## `export const oauthIssuerIdentifierSchema`

This has been converted.

Swift equivalent:

```swift
public struct OAuthIssuerIdentifier: Codable
```
