# Conversion for `oauth-types/src/oauth-endpoint-auth-method.ts`

## `export const oauthEndpointAuthMethod`

This has been converted.

Swift equivalent:

```swift
public enum OAuthEndpointAuthMethod: String, Codable
```
