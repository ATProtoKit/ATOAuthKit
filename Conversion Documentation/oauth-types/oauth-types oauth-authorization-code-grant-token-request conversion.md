# Conversion for `oauth-types/src/oauth-authorization-code-grant-token-request.ts`

## `export const oauthAuthorizationCodeGrantTokenRequestSchema`

This has been converted.

Swift equivalent:

```swift
public struct ClientMetadata: Codable
```
